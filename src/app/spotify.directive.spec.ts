import { SpotifyDirective } from './spotify.directive';

describe('SpotifyDirective', () => {
  it('should create an instance', () => {
    const directive = new SpotifyDirective();
    expect(directive).toBeTruthy();
  });
});
